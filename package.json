{
  "name": "mi-plataforma",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "setup:services": "pnpm --filter dashboard-service run setup && pnpm --filter extractor-service run setup && pnpm --filter ebookforge-service run setup"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
